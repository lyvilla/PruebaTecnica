serenity.browser.maximized=true
headless.mode = false


webdriver {
  driver = chrome
  autodownload = true
  timeouts {
    implicitlywait = 5000
    fluentwait = 10000
  }
  capabilities {
    browserName = "chrome"
    "goog:chromeOptions" {
      args = ["incognito"]
    }
  }
}